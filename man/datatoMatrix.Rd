% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datatoMatrix.R
\name{datatoMatrix}
\alias{datatoMatrix}
\title{Convert Tip Data to a Probability Matrix}
\usage{
datatoMatrix(x, range = NULL, hyper = TRUE, buffer = 0)
}
\arguments{
\item{x}{A data frame with at least 2 columns:
\describe{
\item{Column 1}{Character. Species names.}
\item{Column 2}{Numeric. Chromosome counts (will be rounded to whole
numbers if non-integer).}
\item{Column 3}{Numeric. Probability of being in binary state 1
(required when \code{hyper = TRUE}). Values should be between 0 and 1.}
}}

\item{range}{Numeric vector of length 2 giving the minimum and maximum
chromosome numbers to include in the model, e.g. \code{c(5, 20)}. Defaults to
\code{NULL}, in which case the range is determined from the observed data plus
any \code{buffer}.}

\item{hyper}{Logical. If \code{TRUE} (default), a binary hyperstate is included
and the matrix is doubled in width.}

\item{buffer}{Integer. Number of chromosome states to add above and below the
observed range. Only used when \code{range = NULL}. Defaults to \code{0}.}
}
\value{
A numeric matrix with species as rows and chromosome states as
columns. Column names are the chromosome numbers (and, when
\code{hyper = TRUE}, chromosome numbers suffixed with \code{"h"} for the second
hyperstate). Row names are the species names from \code{x}.
}
\description{
Converts a data frame of species names, chromosome counts, and (optionally)
binary trait probabilities into a probability matrix suitable for use with
diversitree's \code{make.mkn()} or \code{make.musse()} functions.
}
\details{
When \code{hyper = TRUE}, the matrix is doubled to accommodate two hidden states
(hyperstates). Column 3 of the input data is used to assign the probability
of being in hyperstate 1 vs. hyperstate 2 for each species. When
\code{hyper = FALSE}, each species is assigned probability 1 at its observed
chromosome number.
}
\examples{
# Simple example without hyperstate
dat <- data.frame(
  species = c("sp1", "sp2", "sp3"),
  chrom = c(5, 7, 10),
  prob = c(1, 1, 1)
)
datatoMatrix(x = dat, hyper = FALSE)

# With hyperstate and uncertainty in binary trait
dat2 <- data.frame(
  species = c("sp1", "sp2", "sp3"),
  chrom = c(5, 7, 10),
  prob = c(0.8, 0.5, 1.0)
)
datatoMatrix(x = dat2, hyper = TRUE)

}
\seealso{
\code{\link[=constrainMkn]{constrainMkn()}}, \code{\link[=constrainMuSSE]{constrainMuSSE()}} for constraining likelihood
functions built from this matrix.
}
