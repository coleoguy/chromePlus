% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constrainMkn.R
\name{constrainMkn}
\alias{constrainMkn}
\title{Constrain an mkn Likelihood Function for Chromosome Evolution}
\usage{
constrainMkn(
  data,
  lik,
  hyper = T,
  polyploidy = F,
  verbose = F,
  oneway = F,
  constrain = list(drop.poly = F, drop.demi = F, symmetric = F, nometa = F, saf.model =
    F, sym.hyperstates = F, meta = "ARD")
)
}
\arguments{
\item{data}{A probability matrix as produced by \code{\link[=datatoMatrix]{datatoMatrix()}}. Rows are
species, columns are chromosome states (and optionally hyperstate columns).}

\item{lik}{A likelihood function created by \code{diversitree::make.mkn()}.}

\item{hyper}{Logical. If \code{TRUE} (default), includes a binary hyperstate
allowing different rates of chromosome evolution in each state.}

\item{polyploidy}{Logical. If \code{TRUE}, the hyperstate represents ploidy
level (diploid vs. polyploid), and transitions between states also change
chromosome number. Defaults to \code{FALSE}.}

\item{verbose}{Logical. If \code{TRUE}, returns a list containing the
constrained likelihood function, the parameter identity matrix, and the
rate table. Defaults to \code{FALSE}.}

\item{oneway}{Logical. If \code{TRUE}, the transition rate from state 2 back to
state 1 is set to zero. Defaults to \code{FALSE}.}

\item{constrain}{A list of additional model constraints. Can include:
\describe{
\item{drop.poly}{Logical. If \code{TRUE}, polyploidy rate is set to zero.}
\item{drop.demi}{Logical. If \code{TRUE}, demiploidy rate is set to zero.}
\item{symmetric}{Logical. If \code{TRUE}, chromosome change rates are equal
across binary states.}
\item{nometa}{Logical. If \code{TRUE}, chromosome rates are constrained to
be equal across hyperstates.}
\item{meta}{Character. Either \code{"ARD"} (all rates different, default) or
\code{"SYM"} (symmetric transitions between hyperstates).}
\item{saf.model}{Logical. If \code{TRUE}, transitions between hyperstates
follow the sex chromosome-autosome fusion model.}
\item{sym.hyperstates}{Logical. If \code{TRUE}, ascending and descending
rates are equal across hyperstates.}
}}
}
\value{
If \code{verbose = FALSE} (default), returns a constrained likelihood
function compatible with \code{diversitree::find.mle()} and
\code{diversitree::mcmc()}. If \code{verbose = TRUE}, returns a list with elements:
\describe{
\item{\verb{likelihood function}}{The constrained likelihood function.}
\item{\verb{parameter matrix}}{A matrix showing which rate category each
transition belongs to.}
\item{\code{ratetable}}{A data frame mapping transitions to rate names.}
}
}
\description{
Constrains a diversitree \code{mkn} likelihood function to match a biologically
realistic model of chromosome number evolution. Supports three major model
types: a simple chromEvol model (no binary trait), a model where ploidy is
the hidden state, and a model where a binary trait affects chromosome
evolution rates. Additionally supports a sex chromosome-autosome fusion
(SAF) model.
}
\details{
The rates in the model are:
\describe{
\item{asc1/asc2}{Ascending aneuploidy in state 1/2}
\item{desc1/desc2}{Descending aneuploidy in state 1/2}
\item{pol1/pol2}{Polyploidization in state 1/2}
\item{dem1/dem2}{Demipolyploidy in state 1/2}
\item{redip}{Rediploidization}
\item{tran12/tran21}{Transitions between hyperstates}
\item{tranSAF/tranRo}{SAF transitions (sex chromosome-autosome fusions
and Robertsonian transitions)}
}
}
\examples{
\donttest{
library(diversitree)
# Create example data
dat <- data.frame(
  species = paste0("sp", 1:5),
  chrom = c(5, 6, 7, 8, 10),
  prob = c(1, 1, 1, 1, 1)
)
dat.mat <- datatoMatrix(x = dat, hyper = FALSE)
# Create a random tree
tree <- ape::rcoal(5, tip.label = dat$species)
lik <- make.mkn(tree, states = dat.mat, k = ncol(dat.mat),
                strict = FALSE, control = list(method = "ode"))
con.lik <- constrainMkn(data = dat.mat, lik = lik, hyper = FALSE,
                         constrain = list(drop.demi = TRUE))
argnames(con.lik)
}

}
\references{
Blackmon, H., Justison, J., Mayrose, I. and Goldberg, E.E. (2019). Meiotic
drive shapes rates of karyotype evolution in mammals. \emph{Evolution}, 73(3),
511--523.
}
\seealso{
\code{\link[=constrainMuSSE]{constrainMuSSE()}} for the MuSSE (state-dependent diversification)
version, \code{\link[=datatoMatrix]{datatoMatrix()}} for preparing input data.
}
