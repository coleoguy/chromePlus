% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeMknFast.R
\name{make.mkn.fast}
\alias{make.mkn.fast}
\title{Fast Mkn Likelihood for Large Chromosome State Spaces}
\usage{
make.mkn.fast(
  tree,
  data,
  hyper = TRUE,
  polyploidy = FALSE,
  oneway = FALSE,
  state.names = NULL,
  constrain = list(),
  root = c("obs", "flat", "given")
)
}
\arguments{
\item{tree}{A phylogenetic tree of class \code{"phylo"}.}

\item{data}{A probability matrix as produced by \code{\link[=datatoMatrix]{datatoMatrix()}}. Rows are
species (with \code{rownames} matching \code{tree$tip.label}), columns are
chromosome states (and optionally hyperstate columns).}

\item{hyper}{Logical. If \code{TRUE} (default), includes a binary hyperstate
allowing different rates of chromosome evolution in each state.}

\item{polyploidy}{Logical. If \code{TRUE}, the hyperstate represents ploidy
level. Defaults to \code{FALSE}.}

\item{oneway}{Logical. If \code{TRUE}, the transition rate from state 2 back to
state 1 is set to zero. Defaults to \code{FALSE}.}

\item{state.names}{Optional character vector of length 2 giving descriptive
names for the two binary states. When provided, all parameter names use
these labels (e.g. \code{asc.matched} instead of \code{asc1}).}

\item{constrain}{A list of additional model constraints. Same options as
\code{\link[=constrainMkn]{constrainMkn()}}: \code{drop.poly}, \code{drop.demi}, \code{symmetric}, \code{nometa},
\code{meta}, \code{saf.model}, \code{sym.hyperstates}.}

\item{root}{Character. Root prior to use when computing the likelihood.
One of \code{"obs"} (default; use \code{D / sum(D)} at root, matching diversitree's
\code{ROOT.OBS}), \code{"flat"} (uniform), or \code{"given"} (use the \code{root.p} argument
passed to the returned function).}
}
\value{
A function \code{ll(pars, root.p = NULL)} that computes the
log-likelihood. The returned function has class \code{"mkn.fast"},
\code{"function"} and an \code{"argnames"} attribute giving the free-parameter
names. Use \code{argnames(ll)} to retrieve the parameter order.
}
\description{
Builds a likelihood function equivalent to \code{make.mkn()} followed by
\code{\link[=constrainMkn]{constrainMkn()}}, but evaluates the per-call likelihood with a sparse
uniformization-based Felsenstein pruning routine implemented in C++. At
large \code{k} (the number of states), this is typically 30-100x faster than
diversitree's ODE pruning, while producing log-likelihoods that agree to
near machine precision.
}
\details{
The returned function has the same calling convention as a constrained
diversitree likelihood: a numeric \code{pars} vector whose names match
\code{argnames(ll)}. It works directly with \code{diversitree::find.mle()} and
\code{diversitree::mcmc()}.

This function is most useful when the chromosome state space is large
(\code{k >= 100}) and the per-evaluation cost in diversitree is dominated by
branch-wise transition-probability computation. For small \code{k}, the
diversitree path is already fast and the speedup margin is modest.
}
\examples{
\donttest{
library(diversitree); library(ape)
set.seed(1)
tree <- rcoal(50, tip.label = paste0("sp", 1:50))
sim <- simChrom(tree = tree, model = "ChromPlus",
  pars = c(asc1=0.1, asc2=0.1, desc1=0.08, desc2=0.08,
           dem1=0, dem2=0, pol1=0.005, pol2=0.005,
           tran12=0.02, tran21=0.02,
           root.chrom = 12, root.state = 0),
  limits = c(5, 20))
dat <- data.frame(species = tree$tip.label,
                  chrom = as.numeric(sim$chrom.num),
                  prob = as.numeric(sim$binary.state == 0))
dat.mat <- datatoMatrix(x = dat, range = c(5, 20), hyper = TRUE)
ll <- make.mkn.fast(tree, dat.mat, hyper = TRUE,
                   constrain = list(drop.demi = TRUE))
argnames(ll)
pars <- setNames(rep(0.05, length(argnames(ll))), argnames(ll))
ll(pars)
fit <- find.mle(ll, pars, method = "subplex")
}

}
\seealso{
\code{\link[=constrainMkn]{constrainMkn()}} for the diversitree-backed equivalent.
}
