% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeSSEchrom.R
\name{makeSSEchrom}
\alias{makeSSEchrom}
\title{Simulate Chromosome Data Under a Binary SSE Model}
\usage{
makeSSEchrom(
  h,
  lambda1,
  lambda2,
  mu1,
  mu2,
  asc1,
  asc2,
  desc1,
  desc2,
  trans1,
  trans2,
  max.taxa,
  x0,
  state.names = NULL
)
}
\arguments{
\item{h}{Integer. The maximum haploid chromosome number for the simulation.}

\item{lambda1}{Numeric. Speciation rate in binary state 1.}

\item{lambda2}{Numeric. Speciation rate in binary state 2.}

\item{mu1}{Numeric. Extinction rate in binary state 1.}

\item{mu2}{Numeric. Extinction rate in binary state 2.}

\item{asc1}{Numeric. Chromosome gain rate in binary state 1.}

\item{asc2}{Numeric. Chromosome gain rate in binary state 2.}

\item{desc1}{Numeric. Chromosome loss rate in binary state 1.}

\item{desc2}{Numeric. Chromosome loss rate in binary state 2.}

\item{trans1}{Numeric. Transition rate from binary state 1 to state 2.}

\item{trans2}{Numeric. Transition rate from binary state 2 to state 1.}

\item{max.taxa}{Integer. The simulation runs until this many extant taxa
are present.}

\item{x0}{Integer. Starting haploid chromosome number at the root.}

\item{state.names}{An optional character vector of length 2 giving
descriptive names for the two binary states, e.g. \code{c("matched", "mismatched")}. When provided, the \code{binary.char} output uses these
labels instead of 0/1. Defaults to \code{NULL}.}
}
\value{
A list with three elements:
\describe{
\item{tree}{A phylogenetic tree of class \code{"phylo"}.}
\item{binary.char}{A named vector of binary character states for each
tip. If \code{state.names} is provided, values are the descriptive labels;
otherwise integer 0 or 1.}
\item{haploid.num}{A named integer vector of haploid chromosome numbers
for each tip.}
}
}
\description{
Simulates chromosome number evolution alongside a binary trait under a
State-dependent Speciation and Extinction (SSE) model using diversitree's
\code{tree.musse()}. This generates a phylogenetic tree together with tip data
for both chromosome number and binary character state.
}
\details{
The binary state at the root is assigned randomly. Make sure \code{h} is set
high enough that it does not act as a hard barrier on chromosome number
evolution.
}
\examples{
\donttest{
set.seed(42)
result <- makeSSEchrom(
  h = 10, lambda1 = 0.3, lambda2 = 0.3,
  mu1 = 0.1, mu2 = 0.1,
  asc1 = 0.1, asc2 = 0.1,
  desc1 = 0.1, desc2 = 0.1,
  trans1 = 0.05, trans2 = 0.05,
  max.taxa = 30, x0 = 5,
  state.names = c("matched", "mismatched")
)
plot(result$tree)
table(result$binary.char)
}

}
\references{
Blackmon, H., Justison, J., Mayrose, I. and Goldberg, E.E. (2019). Meiotic
drive shapes rates of karyotype evolution in mammals. \emph{Evolution}, 73(3),
511--523.
}
\seealso{
\code{\link[=simChrom]{simChrom()}} for simulating chromosome evolution on a fixed tree.
}
