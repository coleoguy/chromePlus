% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/startVals.R
\name{startVals}
\alias{startVals}
\title{Generate Starting Values from a Distribution}
\usage{
startVals(n, min, max, mean, sd, dist = "unif")
}
\arguments{
\item{n}{Integer. The number of starting values to generate.}

\item{min}{Numeric. The minimum value for a uniform distribution. Required
when \code{dist = "unif"}.}

\item{max}{Numeric. The maximum value for a uniform distribution. Required
when \code{dist = "unif"}.}

\item{mean}{Numeric. The mean for a normal distribution. Required when
\code{dist = "norm"}.}

\item{sd}{Numeric. The standard deviation for a normal distribution. Required
when \code{dist = "norm"}.}

\item{dist}{Character string specifying the distribution. Either \code{"unif"}
(default) for uniform or \code{"norm"} for normal.}
}
\value{
A numeric vector of length \code{n} containing randomly sampled values.
}
\description{
Samples starting values from a uniform or normal distribution for use as
initial parameter values in likelihood optimization or MCMC analysis.
}
\examples{
# Generate 5 uniform starting values between 0 and 1
startVals(n = 5, min = 0, max = 1)

# Generate 5 normal starting values
startVals(n = 5, mean = 0.5, sd = 0.1, dist = "norm")

}
\seealso{
\code{\link[=constrainMkn]{constrainMkn()}}, \code{\link[=constrainMuSSE]{constrainMuSSE()}} for functions that use
starting values in model fitting.
}
