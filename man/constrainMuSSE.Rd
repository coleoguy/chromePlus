% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constrainMuSSE.R
\name{constrainMuSSE}
\alias{constrainMuSSE}
\title{Constrain a MuSSE Likelihood Function for Chromosome Evolution}
\usage{
constrainMuSSE(
  data,
  lik,
  hyper = T,
  polyploidy = F,
  s.lambda = T,
  s.mu = T,
  verbose = F,
  constrain = list(drop.poly = F, drop.demi = F, symmetric = F, nometa = F, meta = "ARD")
)
}
\arguments{
\item{data}{A probability matrix as produced by \code{\link[=datatoMatrix]{datatoMatrix()}}. Rows are
species, columns are chromosome states (and optionally hyperstate columns).}

\item{lik}{A likelihood function created by \code{diversitree::make.musse()}.}

\item{hyper}{Logical. If \code{TRUE} (default), includes a binary hyperstate
allowing different rates of chromosome evolution in each state.}

\item{polyploidy}{Logical. If \code{TRUE}, the hyperstate represents ploidy
level (diploid vs. polyploid). Defaults to \code{FALSE}.}

\item{s.lambda}{Logical. If \code{TRUE} (default), a single speciation rate is
estimated across all states. If \code{FALSE}, separate speciation rates are
estimated for each hyperstate.}

\item{s.mu}{Logical. If \code{TRUE} (default), a single extinction rate is
estimated across all states. If \code{FALSE}, separate extinction rates are
estimated for each hyperstate.}

\item{verbose}{Logical. If \code{TRUE}, returns a list containing the
constrained likelihood function and the parameter identity matrix.
Defaults to \code{FALSE}.}

\item{constrain}{A list of additional model constraints. Can include:
\describe{
\item{drop.poly}{Logical. If \code{TRUE}, polyploidy rate is set to zero.}
\item{drop.demi}{Logical. If \code{TRUE}, demiploidy rate is set to zero.}
\item{symmetric}{Logical. If \code{TRUE}, chromosome change rates are equal
across binary states.}
\item{nometa}{Logical. If \code{TRUE}, chromosome rates are constrained to
be equal across hyperstates.}
\item{meta}{Character. Either \code{"ARD"} (all rates different, default) or
\code{"SYM"} (symmetric transitions between hyperstates).}
}}
}
\value{
If \code{verbose = FALSE} (default), returns a constrained likelihood
function compatible with \code{diversitree::find.mle()} and
\code{diversitree::mcmc()}. If \code{verbose = TRUE}, returns a list with the
constrained likelihood function and parameter identity matrix.
}
\description{
Constrains a diversitree \code{MuSSE} (Multi-State Speciation and Extinction)
likelihood function to model chromosome number evolution with
state-dependent diversification. This extends \code{\link[=constrainMkn]{constrainMkn()}} by allowing
speciation and extinction rates to differ between binary states.
}
\details{
The chromosome evolution rates are the same as in \code{\link[=constrainMkn]{constrainMkn()}}:
ascending/descending aneuploidy, polyploidy, demiploidy, and transitions
between hyperstates. In addition, this function constrains speciation
(\code{lambda}) and extinction (\code{mu}) rates, which can either be shared across
states or estimated separately depending on \code{s.lambda} and \code{s.mu}.
}
\examples{
\donttest{
library(diversitree)
# Create example data with uncertainty in binary trait
dat <- data.frame(
  species = paste0("sp", 1:5),
  chrom = c(5, 6, 7, 8, 10),
  prob = c(0.8, 0.6, 0.9, 0.5, 1.0)
)
dat.mat <- datatoMatrix(x = dat, hyper = TRUE)
tree <- ape::rcoal(5, tip.label = dat$species)
lik <- make.musse(tree, states = dat.mat, k = ncol(dat.mat),
                  strict = FALSE, control = list(method = "ode"))
con.lik <- constrainMuSSE(data = dat.mat, lik = lik,
                           s.lambda = FALSE, s.mu = FALSE,
                           constrain = list(drop.demi = TRUE))
argnames(con.lik)
}

}
\references{
Blackmon, H., Justison, J., Mayrose, I. and Goldberg, E.E. (2019). Meiotic
drive shapes rates of karyotype evolution in mammals. \emph{Evolution}, 73(3),
511--523.
}
\seealso{
\code{\link[=constrainMkn]{constrainMkn()}} for the mkn (no diversification) version,
\code{\link[=datatoMatrix]{datatoMatrix()}} for preparing input data.
}
